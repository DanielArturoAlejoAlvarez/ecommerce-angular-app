import { CompareWithDirective } from './compare-with.directive';

describe('CompareWithDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareWithDirective();
    expect(directive).toBeTruthy();
  });
});
